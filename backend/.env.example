# 호스트에서 backend 를 직접 실행할 때 사용 (1단계 Day 2~3).
# `cp .env.example .env` 후 필요 시 수정.
#
# 15432 = compose.yaml 이 호스트에 노출한 PostGIS 포트.
# Compose 안 API 컨테이너는 루트 `.env.example` 의 `@db:5432` 를 쓴다.
DATABASE_URL=postgresql+asyncpg://farmtwin:farmtwin@127.0.0.1:15432/farmtwin
