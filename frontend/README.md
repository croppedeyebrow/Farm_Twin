# frontend

FarmTwin 관제 UI (React + Vite + TypeScript).

- Node.js **22** (`../.nvmrc`)
- 패키지 관리: Yarn

```bash
yarn
yarn dev
```

3D 재배실(Three.js/R3F)은 1단계 Day 3에서 추가한다.

개발 서버는 Vite proxy로 `/api/*` → `http://127.0.0.1:8000/*` 를 연결한다.

